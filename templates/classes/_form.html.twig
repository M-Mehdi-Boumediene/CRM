{{ form_start(form) }}

<div class="form-row">
	<div class="col-md-3">
		{{ form_row(form.nom,{'attr': {'class': 'form-control'}}) }}
		{{ form_errors(form.nom) }}
	</div>
	<div class="col-md-3">
		{{ form_row(form.anneescolaire,{'attr': {'class': 'form-control'}}) }}
		{{ form_errors(form.anneescolaire) }}
	</div>
	<div class="col-md-3">
		{{ form_row(form.curus,{'attr': {'class': 'form-control'}}) }}
		{{ form_errors(form.curus) }}
	</div>
	<div class="col-md-3">
		{{ form_row(form.nbsemestre,{'attr': {'class': 'form-control'}}) }}
		{{ form_errors(form.nbsemestre) }}
	</div>
</div>


<br>
<button type="submit" class="mt-1 btn btn-success">{{ button_label|default('Sauvegarder') }}</button>
ou
<a href="{{ path('app_classes_index') }}">Retour à la liste</a>

{{ form_end(form) }}
