<form method="post" action="{{ path('app_tableau_notes_delete', {'id': tableau_note.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tableau_note.id) }}">
    <button class="btn">Delete</button>
</form>
