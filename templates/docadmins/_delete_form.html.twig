<form method="post" action="{{ path('app_docadmins_delete', {'id': docadmin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ docadmin.id) }}">
    <button class="btn">Delete</button>
</form>
